# Spec: Fix low: upgrade on-headers to ≥ 1.1.0 and cookie to ≥ 0.7.0
**Issue**: #59
**Date**: 2026-08-01
**Rework**: Yes — previous implementation added overrides to package.json but did not run `npm install` / `npm audit fix` to actually regenerate package-lock.json and resolve the lock-file entries.

## Approach
The `package.json` overrides block already contains the correct entries:
- `"on-headers": ">=1.1.0"`
- `"cookie": ">=0.7.0"`

The root cause of the dev-rework is that `package-lock.json` was not regenerated after those overrides were added in the previous round. The fix is to run `npm audit fix` (which resolves non-breaking advisories and regenerates the lock file), then confirm via `npm ls on-headers cookie` that both resolve to compliant versions, and verify `npm run build` exits 0.

After `npm audit fix`:
- `on-headers` resolves to `1.1.0` (satisfies ≥ 1.1.0) ✅
- `cookie` resolves to `2.0.1` (satisfies ≥ 0.7.0) ✅
- Neither `on-headers` nor `cookie` appears in `npm audit` output ✅
- `npm run build` exits 0 ✅

## Files to Change
- `package-lock.json`: Regenerated by `npm audit fix` to reflect compliant resolved versions of `on-headers` and `cookie`. This is the file that was missing its update in the prior round.
- `package.json`: No changes needed — overrides were already correct from the prior implementation.

## Files NOT to Change
- `src/` (any file): Out of scope per story. No app source code changes.
- `angular.json`: No build configuration changes needed.
- `tsconfig*.json`: Not relevant to dependency pinning.
- `karma.conf.js`: Not relevant.

## Risks / Assumptions
- `npm audit fix` also upgraded `webpack-dev-server` from 5.2.6 → 6.0.0 as a side-effect of resolving other advisories; this is a transitive toolchain dep and the build continues to pass.
- The remaining 8 moderate advisories reported by `npm audit` (for `@hono/node-server`, `ajv`, `uuid`) are out of scope for this story — they require breaking-change upgrades or have no available fix.
- No app source files were changed.
